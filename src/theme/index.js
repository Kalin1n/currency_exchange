import {createMuiTheme} from "@material-ui/core"

const baseTheme = createMuiTheme({
  palette:{
    primary:{
      main: "#34C329",
      secondary:"#119507"
    }
  }
});

export default baseTheme;